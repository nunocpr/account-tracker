export enum ActionType {
    // eslint-disable-next-line no-unused-vars
    Add = "add",
    // eslint-disable-next-line no-unused-vars
    Edit = "edit",
    // eslint-disable-next-line no-unused-vars
    Remove = "remove",
}
